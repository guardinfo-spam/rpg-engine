using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace RpgGamePcl.Combat
{
    public sealed class CombatEnums
    {
        public enum ActorController
        {
            Player = 0,
            AI = 1
        }

        public enum SideEnum
        {
            Left = 0,
            Right = 1
        }
    }
}
